doctype html
html
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Shop #{shopName} vừa tung mã giảm giá mới!
  body(style="font-family: Arial, sans-serif; margin: 0; padding: 0; background-color: #f6fafd;")
    .email-container(style="max-width: 600px; margin: auto; background: #fff; padding: 22px; border-radius: 8px; box-shadow: 0 2px 8px #e2e2e2;")
      .email-header(style="text-align: center; margin-bottom: 18px;")
        img.logo(src=logo alt="Logo" style="max-width: 120px;")
      .email-body
        h2(style="color: #e67e22;") Ưu đãi mới từ shop #{shopName}!
        p(style="font-size: 16px; color: #444;") Xin chào #{firstName},
        p Shop #{shopName} vừa có mã giảm giá mới dành riêng cho bạn:
        .discount-box(style="background: #ffe0b2; color: #c76c11; padding: 18px; margin: 18px 0; border-radius: 6px; font-size: 18px; text-align: center;")
          strong #{discountCode} – Giảm #{discountValue}
          if discountMinOrder
            br
            | Đơn tối thiểu: #{discountMinOrder}
          if discountEndDate
            br
            | Hạn sử dụng: #{discountEndDate}
        if discountDesc
          p(style="font-size: 15px; color: #555; margin-bottom: 8px;") #{discountDesc}
        a.btn(href=shopLink style="display:inline-block; margin-top:18px; padding:10px 22px; background-color:#e67e22; color:#fff; text-decoration:none; border-radius:4px;") Xem shop & mua ngay
      .email-footer(style="margin-top: 32px; text-align: center; font-size: 13px; color: #aaa;")
        p Nhanh tay sử dụng ưu đãi, số lượng có hạn!
